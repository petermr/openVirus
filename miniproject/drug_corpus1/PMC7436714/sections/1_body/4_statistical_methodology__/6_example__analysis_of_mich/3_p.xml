<?xml version="1.0" encoding="UTF-8"?>
<p>In the above program, we consider a time‐dependent declining transmission rate with the modifier value 
 <math id="nlm-math-72">
  <mi>π</mi>
  <mo stretchy="false">(</mo>
  <mi>t</mi>
  <mo stretchy="false">)</mo>
  <mo>=</mo>
  <mi>exp</mi>
  <mo stretchy="false">(</mo>
  <mo>−</mo>
  <msub>
   <mrow>
    <mi>λ</mi>
   </mrow>
   <mrow>
    <mn>0</mn>
   </mrow>
  </msub>
  <mi>t</mi>
  <mo stretchy="false">)</mo>
 </math> where the parameter 
 <italic>λ</italic>
 <sub>0</sub> is chosen so that the modifier equals to 0.6 on 2 May. This value is determined based on the social distancing scoreboard posted by Unacast, Inc. (
 <ext-link ext-link-type="uri" xlink:href="https://www.unacast.com/covid19/social-distancing-scoreboard" xmlns:xlink="http://www.w3.org/1999/xlink">https://www.unacast.com/covid19/social‐distancing‐scoreboard</ext-link>). One needs to set 
 <monospace>exponential = TRUE</monospace>, to activate such setting. Alternatively, as shown in Figure 
 <xref rid="insr12402-fig-0010" ref-type="fig">10(A)</xref>–(C), one may use a step function by providing a vector of 
 <monospace>pi0</monospace>, values and the corresponding vector of changes dates in 
 <monospace>change_time</monospace>. In the main function above, we let the starting date be 11 March and conduct the estimation and projection of 200 days ahead (
 <monospace>T_fin = 200</monospace>) on 10 June and after. We run four separate MCMC chains with different initial values, each with length of 5×10
 <sup>5</sup>, kept from every 10 draws (
 <monospace>thn = 10</monospace>) (a thinning operation to reduce autocorrelations) after 2×10
 <sup>5</sup> draws are dropped. Thus, with a relatively squandering setting, we expect a better performance of convergence and reliable quantification of prediction uncertainty using sample quantiles.
</p>
