<?xml version="1.0" encoding="UTF-8"?>
<p>In the nearest neighbor condition, when a patch’s threshold for control was exceeded, the focal patch was treated the next day and its nearest neighbors were treated one day later. In the second nearest neighbor condition, when a patch’s threshold for control was exceeded, the focal patch, its nearest neighbors, and its second nearest neighbors were all treated, with nearest neighbors treated the day after the focal patch and second nearest neighbors treated one day after that.</p>
