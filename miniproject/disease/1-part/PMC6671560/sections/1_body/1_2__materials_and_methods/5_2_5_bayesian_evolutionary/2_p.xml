<?xml version="1.0" encoding="UTF-8"?>
<p>To model the nucleotide substitution process, the codon positions were partitioned into first + second vs. third positions. A separate HKY85 substitution model was applied, with a discretized Γ distribution (HKY + Γ) to model rate heterogeneity across sites to each of the two codon partitions (
 <xref rid="vez023-B64" ref-type="bibr">Shapiro, Rambaut, and Drummond 2006</xref>). To accommodate among-lineage rate variation, an uncorrelated relaxed molecular clock that models the branch rate variation according to a lognormal distribution was specified (
 <xref rid="vez023-B24" ref-type="bibr">Drummond et al. 2006</xref>). The flexible nonparametric demographic skygrid prior was selected (
 <xref rid="vez023-B30" ref-type="bibr">Gill et al. 2013</xref>). Stationarity and mixing (e.g. based on effective sample sizes &gt;200 for the continuous parameters) were examined using Tracer version 1.6. The MCC trees were summarized using TreeAnnotator.
</p>
