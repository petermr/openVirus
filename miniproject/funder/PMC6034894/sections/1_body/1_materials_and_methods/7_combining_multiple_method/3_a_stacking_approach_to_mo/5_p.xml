<?xml version="1.0" encoding="UTF-8"?>
<p>When 
 <italic>e</italic> is mean absolute error: 
 <list list-type="bullet">
  <list-item>
   <p>We choose 
    <italic>μ</italic> = 0 (and further, exclude the uniform distribution method from the ensemble entirely).
   </p>
  </list-item>
  <list-item>
   <p>This optimization problem is referred to as least absolute deviation regression or median regression, with linear inequality and equality constraints on the coefficients; we reformulate the problem as a linear program and use the 
    <monospace>lpSolve</monospace> package [
    <xref rid="pcbi.1006134.ref049" ref-type="bibr">49</xref>] to find a solution.
   </p>
  </list-item>
 </list>
</p>
