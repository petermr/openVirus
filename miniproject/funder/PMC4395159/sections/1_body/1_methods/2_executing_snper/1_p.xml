<?xml version="1.0" encoding="UTF-8"?>
<p>After installing the SNPer package, users can use SNPer to compare IFV SNPs by executing the SNPer function. An example of SNPer usage is shown in 
 <xref rid="pone.0122812.g001" ref-type="fig">Fig 1</xref>. The user can change the variables including 
 <italic>input_files_list</italic> (a file contained the list of SNPs files), 
 <italic>mysql_user</italic> (a user name in MySQL), 
 <italic>mysql_password</italic> (a password for the user name), 
 <italic>mysql_host</italic> (the host name of MySQL), and 
 <italic>db_name</italic> (the database name for storing SNPs data). The SNPer database must be created by the user before executing SNPer.
</p>
