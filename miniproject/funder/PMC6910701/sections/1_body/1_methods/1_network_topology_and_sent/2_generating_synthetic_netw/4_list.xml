<?xml version="1.0" encoding="UTF-8"?>
<list list-type="simple">
 <list-item>
  <p>
   <bold>Modular</bold>: We create 
   <italic>m</italic> modules, each consisting of 
   <italic>n</italic> nodes. Every stub is chosen to be either 
   <italic>intra-module</italic>, with probability 
   <italic>p</italic> (the 
   <italic>module assortativity</italic>), or 
   <italic>inter-module</italic>, with probability 1 − 
   <italic>p</italic>. Intra-module stubs can only be paired with stubs belonging to nodes of the same module, inter-module stubs can only be paired with other inter-module stubs.
  </p>
 </list-item>
 <list-item>
  <p>
   <bold>Spatial</bold>: Nodes are placed on a circle evenly spaced with distance of 1 between them (around the circumference). Two stubs can only be paired together if they are withing a distance 
   <italic>r</italic> (the 
   <italic>connection radius</italic>) of each other.
  </p>
 </list-item>
</list>
