<?xml version="1.0" encoding="UTF-8"?>
<p>Data analysed in the package need to be transformed into a standardised format. The default input is in form of weekly data with each row representing the data for that week. Other dates can be used when using the lower level functions provided by the package. Columns contain the data stratified by age group. If the data are also stratified by risk group then the first group of columns will hold the data for one risk group, stratified by age group. The next set of columns will be the next risk group by age group, etc. The input as used in the examples consist of (see 
 <xref ref-type="fig" rid="pcbi.1005838.g001">Fig 1</xref>):
</p>
