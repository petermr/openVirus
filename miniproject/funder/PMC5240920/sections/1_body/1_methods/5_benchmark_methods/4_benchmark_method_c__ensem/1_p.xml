<?xml version="1.0" encoding="UTF-8"?>
<p>We selected the ensemble Kalman Filter (EnKF) [
 <xref rid="pcbi.1005257.ref027" ref-type="bibr">27</xref>] for the third benchmark method. EnKF does not use a likelihood function 
 <italic>L</italic>(
 <italic>Y</italic>
 <sub>
  <italic>i</italic>
 </sub>; 
 <italic>θ</italic>) and instead it propagates an initial ensemble that contains both epidemic states and epidemic parameters. Each new observation is then used to update this ensemble. After such updating, the ensemble is considered as a sample from the posterior distribution for both the parameters and the states.
</p>
