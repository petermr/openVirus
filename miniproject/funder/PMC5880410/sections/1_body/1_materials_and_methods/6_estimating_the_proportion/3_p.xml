<?xml version="1.0" encoding="UTF-8"?>
<p>One way to obtain 
 <italic>y</italic> is to scale to an experimentally determined value for 
 <italic>β</italic>, given a specific ethnicity and viral strain (
 <italic>E</italic>
 <sub>0</sub>, 
 <italic>V</italic>
 <sub>0</sub>). Values for 
 <italic>β</italic> have historically been estimated using techniques such as serotyping the same set of people at different time points to estimate the change in the fraction of individuals susceptible to a given pathogen. Other methods are reviewed in [
 <xref rid="pcbi.1006069.ref060" ref-type="bibr">60</xref>]. Once we have a value of 
 <italic>β</italic> for one epidemic pair (
 <italic>E</italic>
 <sub>0</sub>, 
 <italic>V</italic>
 <sub>0</sub>), we can calculate values 
 <italic>x</italic>
 <sub>
  <italic>i</italic>
 </sub> and 
 <italic>e</italic>
 <sub>
  <italic>i</italic>
 </sub> for this epidemic pair using the HLA genotype generation, epitope prediction and clustering methods outlined above. These can be inserted into 
 <xref ref-type="disp-formula" rid="pcbi.1006069.e016">Eq (14)</xref>, allowing us to compute the value 
 <italic>y</italic>, which we have assumed to be the same across all epidemic pairs. Values of 
 <italic>x</italic>
 <sub>
  <italic>i</italic>
 </sub> and 
 <italic>e</italic>
 <sub>
  <italic>i</italic>
 </sub> for each pair (
 <italic>E</italic>, 
 <italic>V</italic>) can be used, together with this value of 
 <italic>y</italic>, to get a 
 <italic>β</italic> for any pair (
 <italic>E</italic>, 
 <italic>V</italic>).
</p>
