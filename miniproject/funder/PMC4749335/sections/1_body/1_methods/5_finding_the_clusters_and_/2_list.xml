<?xml version="1.0" encoding="UTF-8"?>
<list list-type="order">
 <list-item>
  <p>A clade whose elements are separated by a fixed tree distance of at most 
   <italic>x</italic>, where tree distance is the sum of branch lengths between any two tips [
   <xref rid="pone.0148459.ref012" ref-type="bibr">12</xref>],
  </p>
 </list-item>
 <list-item>
  <p>A clade whose elements are separated by a fixed distance of at most 
   <italic>x</italic>, where distance is the standardized number of different nucleotides between any two sequences [
   <xref rid="pone.0148459.ref027" ref-type="bibr">27</xref>],
  </p>
 </list-item>
 <list-item>
  <p>A clade whose elements are separated by a median pairwise tree distance below 
   <italic>x</italic>, an arbitrary percentile of the tree’s between-tip tree distance distribution [
   <xref rid="pone.0148459.ref028" ref-type="bibr">28</xref>], where tree distance once again corresponds to the sum of branch lengths,
  </p>
 </list-item>
 <list-item>
  <p>A clade in a dendrogram, that is, an ultrametric tree obtained from the matrix of between-tip tree distances, cut at height 
   <italic>x</italic>, where the dendrogram is obtained using one of three methods: average linkage, complete linkage, or 
   <italic>Weighted Pair-Group Method of Analysis</italic> (WPGMA),
  </p>
 </list-item>
</list>
