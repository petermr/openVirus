<?xml version="1.0" encoding="UTF-8"?>
<p>In order to forecast ILI, Total INF, INF A, and INF B of next season (after 26 weeks) in the U.S., we developed prediction models using an artificial neural network (ANN), LR, and ARIMA including exogenous variables (ARIMAX). ANN is an artificial intelligence technology inspired by the architecture of biological neurons, such as that in the human brain [
 <xref rid="pone.0220423.ref023" ref-type="bibr">23</xref>]. The input layer receives an input signal, which moves to the next layer as a modified version of the input signal. It passes through several layers composed of multiple transformations, and last passes through the output layer as an output signal [
 <xref rid="pone.0220423.ref024" ref-type="bibr">24</xref>,
 <xref rid="pone.0220423.ref025" ref-type="bibr">25</xref>]. We implemented ANN using the python library Keras (version 2.2.0) with TensorFlow (version 1.8.0) backend. The scikit–learn library (
 <ext-link ext-link-type="uri" xlink:href="https://scikit-learn.org/stable/" xmlns:xlink="http://www.w3.org/1999/xlink">https://scikit-learn.org/stable/</ext-link>) was used for data management and preprocessing. In this study, we used a three-layer ANN network with a 10% dropout rate for the first layer for the overfitting problem. The models were optimized using the Adam optimizer with a loss function of mean squared error. Neuron activation functions were rectified linear units for the second layer. We selected 100 epochs and one batch size for the ANN model.
</p>
