<?xml version="1.0" encoding="UTF-8"?>
<p>In addition, we also computed other models that have been applied in dengue prediction. The BPNN model has shown excellent performance in multivariate time series prediction. For the BPNN model, an optimal parameter (include the number of neurons in the hidden layer and learning rate) was selected to avoid overfitting and improve the predictive performance [
 <xref rid="B32-ijerph-17-00453" ref-type="bibr">32</xref>]. For the SVR model, we considered using an ε-SVR approach, which uses a linear kernel function to track dengue dynamics [
 <xref rid="B46-ijerph-17-00453" ref-type="bibr">46</xref>]. For the GAM model [
 <xref rid="B47-ijerph-17-00453" ref-type="bibr">47</xref>] and the GBM model [
 <xref rid="B48-ijerph-17-00453" ref-type="bibr">48</xref>], the parameters of training used the default values in the python package.
</p>
