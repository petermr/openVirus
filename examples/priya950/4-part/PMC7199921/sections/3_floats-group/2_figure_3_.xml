<?xml version="1.0" encoding="UTF-8"?>
<fig id="F3" orientation="portrait" position="float">
 <label>Figure 3:</label>
 <caption>
  <title>Offering mosquitoes a second bloodmeal post-infection decreases mean EIP and increases transmission potential of ZIKV.</title>
  <p id="P53">(a) Data from our single-feed disseminated infection experiments (
   <styled-content style="color:#0276FD">●</styled-content>) and double-feed disseminated infection experiments (
   <styled-content style="color:#FF0000">●</styled-content>) are shown with respective fitted gamma cumulative distribution function (CDF) ( (
   <styled-content style="color:#0276FD">−</styled-content>) single-feed,; (
   <styled-content style="color:#FF0000">−</styled-content>) double-feed). Each data point represents the proportion of infected mosquitoes at a single time point in a given experiment; the radii correspond proportionally to both the sample size and weight in estimating the parameters of the CDF. For reference, we also estimated ZIKV dissemination rates in 
   <italic>Aedes aegypti</italic> mosquitoes offered only one infectious bloodmeal based on data from seven published studies along with our own single-feed results (
   <styled-content style="color:#8B8989">●</styled-content>), and fitted a gamma CDF to the aggregated data (gray line). (b) Posterior densities for the single-feed EIP (
   <styled-content style="color:#0276FD">−</styled-content>) and double-feed EIP (
   <styled-content style="color:#FF0000">−</styled-content>) are plotted with corresponding means of the respective EIP shown by the dashed lines. (c) The boxplots display the distribution of mean 
   <italic>R</italic>
   <sub>0</sub> values for mosquitos offered a single bloodmeal (blue) versus a second bloodmeal (red). The boxes represent the interquartile range of the mean 
   <italic>R</italic>
   <sub>0</sub> values, the black line within the box marks the median, and the whiskers indicate the 10
   <sup>th</sup> and 90
   <sup>th</sup> percentiles of the mean 
   <italic>R</italic>
   <sub>0</sub> values.
  </p>
 </caption>
 <graphic xlink:href="nihms-1541490-f0003" xmlns:xlink="http://www.w3.org/1999/xlink"/>
</fig>
